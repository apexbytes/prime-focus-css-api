/tmp
/out-tsc

/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.pnp
.pnp.js

.vscode/*
# build output
/dist

# env
.env
.env.*
!.env.example

# logs
/logs
*.log

# local attachment storage — real customer documents land here when no object
# store is configured, so it must never be committable
/storage

# test / tooling caches
/coverage
.eslintcache
.vitest
